<UserControl x:Class="ConnectionIomGenerator.UI.Views.ConnectionInputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ConnectionIomGenerator.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<DockPanel LastChildFill="True">
			<ToolBarTray DockPanel.Dock="Top">
				<ToolBar Band="1" DockPanel.Dock="Top">
					<Button Command="{Binding GenerateIomCommand, Mode=OneWay}">Generate IOM</Button>
				</ToolBar>
			</ToolBarTray>
			<StackPanel DockPanel.Dock="Bottom">
				<TextBlock Text="{Binding Status}"></TextBlock>
			</StackPanel>
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition></ColumnDefinition>
					<ColumnDefinition Width="Auto"></ColumnDefinition>
					<ColumnDefinition></ColumnDefinition>
				</Grid.ColumnDefinitions>

				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="4*"></RowDefinition>
						<RowDefinition Height="Auto"></RowDefinition>
						<RowDefinition Height="1*"></RowDefinition>
					</Grid.RowDefinitions>
			
					<ScrollViewer VerticalScrollBarVisibility="Auto">
						<TextBox Text="{Binding ConnectionDefinitionJson, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
					</ScrollViewer>

					<GridSplitter HorizontalAlignment="Stretch"
										VerticalAlignment="Center"
										Grid.Row="1"
										Height="5">
					</GridSplitter>

					<ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Row="2">
						<TextBox Text="{Binding MessageText, Mode=OneWay}" IsReadOnly="True"></TextBox>
					</ScrollViewer>

				</Grid>

				<GridSplitter HorizontalAlignment="Center"
										VerticalAlignment="Stretch"
										Grid.Column="1"
										Width="5">
				</GridSplitter>

				<ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Column="2">
					<TextBox Text="{Binding IomXml, Mode=OneWay}" IsReadOnly="True"></TextBox>
				</ScrollViewer>
			</Grid>
		</DockPanel>
	</Grid>
</UserControl>
